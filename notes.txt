1) npx create-react-app my-app
2) Boostwatch.com -> download Lux css -> added in src folder https://mdbootstrap.com/docs/b5/react/
3) npm install react-boostrap boostrap 
4) https://react-bootstrap.github.io/components/alerts Bootstrap components
5) font-awsome https://cdnjs.com/
6) npm install axios for fetch backend data

env\Scripts\activate


DJANGO 
to use image in models install pillow
pip install pillow


1)Jakiej wersji react'a używasz

2)Gdy stworze se komponent kropki który bierze 4 agrumenty (a,b,c,d) i zrobię by zwracał mi podświetlone kropki 

            <Nav.Item>
                {a ? (
                    <LinkContainer to='/login'>
                    <Nav.Link>Login</Nav.Link>
                </LinkContainer>
                ):(
                    <Nav.Link disabled>Login</Nav.Link>
                )}
            </Nav.Item>
i tak dla każdego argumentu i go wywołuje na jakimś screenie 
np :
<StworzonyKomponent a/> to powinna mi się wyświeltać jedna kropka 

admin 
admin123